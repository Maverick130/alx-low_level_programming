#include "main.h"
/**
 * _isdigit - cheacks for a digit from 0 to 9
 * @c: variable for the integer
 * Return: 0 success
 *
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}
